<MudTable Class="my-5" Items="@institutions" Hover="true" Filter="new Func<Institution,bool>(FilterFunc)" @bind-SelectedItem="selectedInstitution">
    <ToolBarContent>
        <MudIconButton Icon="@Icons.Material.AddBox" Color="Color.Primary" Size="Size.Large" @onclick="AddInstitution" Class="mr-3" />
        <MudText Typo="Typo.h6">Учебные заведения</MudText>
        <MudToolBarSpacer />
        <MudTextField @bind-Value="searchString" Placeholder="Поиск" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
    </ToolBarContent>
    <HeaderContent>
        <MudTh>Название</MudTh>
        <MudTh>Логотип</MudTh>
        <MudTh></MudTh>
        <MudTh></MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Название">@context.Name</MudTd>
        <MudTd DataLabel="Логотип"><img src="/@imgPath/@context.Logo"></MudTd>
        <MudTd>
            <MudIconButton Icon="@Icons.Material.Edit" Color="Color.Info" OnClick="@((e) => UpdateInstitution(context))" />
        </MudTd>
        <MudTd>
            <MudIconButton Icon="@Icons.Material.Delete" Color="Color.Error" OnClick="@((e) => DeleteInstitution(context))" />
        </MudTd>
    </RowTemplate>
</MudTable>